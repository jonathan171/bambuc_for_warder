<form method="post" action="{{ path('app_tarifas_delete', {'id': tarifa.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tarifa.id) }}">
    <button class="btn">Delete</button>
</form>
