<form method="post" action="{{ path('app_recibo_caja_delete', {'id': recibo_caja.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recibo_caja.id) }}">
    <button class="btn">Delete</button>
</form>
