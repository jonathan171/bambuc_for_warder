{% extends 'base.html.twig' %}

{% block title %}ReciboCaja{% endblock %}

{% block body %}
    <h1>ReciboCaja</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recibo_caja.id }}</td>
            </tr>
            <tr>
                <th>Numero_recibo</th>
                <td>{{ recibo_caja.numeroRecibo }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ recibo_caja.fecha ? recibo_caja.fecha|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recibo_caja_index') }}">back to list</a>

    <a href="{{ path('app_recibo_caja_edit', {'id': recibo_caja.id}) }}">edit</a>

    {{ include('recibo_caja/_delete_form.html.twig') }}
{% endblock %}
