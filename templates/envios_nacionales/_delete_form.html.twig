<form method="post" action="{{ path('app_envios_nacionales_delete', {'id': envios_nacionale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ envios_nacionale.id) }}">
    <button class="btn waves-effect waves-light btn-danger">Borrar</button>
</form>
