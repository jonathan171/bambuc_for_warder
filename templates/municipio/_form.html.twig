
{{ form_start(form) }}
   
    <div class="form-group mb-3 row pb-3">
            <label  class="col-sm-3 text-end control-label col-form-label">codigo</label>
            <div class="col-sm-9">
              {{form_widget(form.codigo) }}
            </div> 
    </div>
    <div class="form-group mb-3 row pb-3">
            <label  class="col-sm-3 text-end control-label col-form-label">nombre</label>
            <div class="col-sm-9">
              {{form_widget(form.nombre) }}
            </div> 
    </div>
     <div class="form-group mb-3 row pb-3">
            <label  class="col-sm-3 text-end control-label col-form-label">departamento</label>
            <div class="col-sm-9">
              {{form_widget(form.departamento) }}
            </div> 
    </div>
   <div class="p-3">
        <div class="form-group text-end">
        <button class="btn btn-info rounded-pill px-4 waves-effect waves-light">{{ button_label|default('Guardar') }}</button>
         <a class="btn btn-dark rounded-pill px-4 waves-effect waves-light" href="{{ path('app_municipio_index') }}">regresar</a>
        </div>
</div>
{{ form_end(form) }}
