<form method="post" action="{{ path('app_factura_resolucion_delete', {'id': factura_resolucion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ factura_resolucion.id) }}">
    <button class="btn">Delete</button>
</form>
