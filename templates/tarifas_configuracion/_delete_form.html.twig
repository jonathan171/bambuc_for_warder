<form method="post" action="{{ path('app_tarifas_configuracion_delete', {'id': tarifas_configuracion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tarifas_configuracion.id) }}">
    <button class="btn">Delete</button>
</form>
