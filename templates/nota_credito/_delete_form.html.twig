<form method="post" action="{{ path('app_nota_credito_delete', {'id': nota_credito.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nota_credito.id) }}">
    <button class="btn">Delete</button>
</form>
