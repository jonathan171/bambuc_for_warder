{% extends 'base.html.twig' %}

{% import "macros/select2.html.twig" as macroselect2 %}

{% block title %}Edit Envio{% endblock %}

{% block stylesheets %}
{{ macroselect2.styleSelect2}}
{% endblock %}

{% block body %}
    <h1>Editar Envio</h1>

    {{ include('envio/_form.html.twig', {'button_label': 'Actualizar'}) }}

   

    {{ include('envio/_delete_form.html.twig') }}
   
{% endblock %}
{% block javascripts %} 
{{ macroselect2.scriptSelect2}}
      <script>
    function CambiarValor(id){
       $.ajax({
        url : '{{ path('app_envio_actualizarvalor')}}',
        data : { id : id},
        type : 'POST',
        dataType : 'json',
        success : function(json) {

            console.log(json);

        alert('valor actualizado Correctamente');
        location.reload();

            },
       error : function(xhr, status) {
        alert('Disculpe, existió un problema');
       }
          });

    }

    function CambiarValorEspecial(id){
       $.ajax({
        url : '{{ path('app_envio_actualizar_valor_especial')}}',
        data : { id : id},
        type : 'POST',
        dataType : 'json',
        success : function(json) {

            console.log(json);

        alert('valor actualizado Correctamente');
        location.reload();

            },
       error : function(xhr, status) {
        alert('Disculpe, existió un problema');
       }
          });

    }
    function CambiarValorEspecialReal(id){
       $.ajax({
        url : '{{ path('app_envio_actualizar_valor_especial_real')}}',
        data : { id : id},
        type : 'POST',
        dataType : 'json',
        success : function(json) {

            console.log(json);

        alert('valor actualizado Correctamente');
        location.reload();

            },
       error : function(xhr, status) {
        alert('Disculpe, existió un problema');
       }
          });

    }
    function CambiarValorEspecial(id){
       $.ajax({
        url : '{{ path('app_envio_actualizar_valor_especial')}}',
        data : { id : id},
        type : 'POST',
        dataType : 'json',
        success : function(json) {

            console.log(json);

        alert('valor actualizado Correctamente');
        location.reload();

            },
       error : function(xhr, status) {
        alert('Disculpe, existió un problema');
       }
          });

    }
    $("#envio_paisOrigen").select2({
            width: '100%'});
            
    $("#envio_paisDestino").select2({
            width: '100%'});

    $(document).on('select2:open', () => {
        document.querySelector('.select2-search__field').focus();
     });

    </script>
{% endblock %}